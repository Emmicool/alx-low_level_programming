#include <stdio.h>

/**
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int num;
	char alph;

	num = '0';
	alph = 'a';
	while (num <= '9')
	{
		putchar(num);
		num++;
	}
	while (alpha <= 'f')
	{
	putchar(alpha);
	alpha++;
	}
		putchar(alpha);
		alpha++;
	}
	putchar('\n');
	return (0);
}
